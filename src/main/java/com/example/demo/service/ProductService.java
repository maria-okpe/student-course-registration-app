/*package com.example.demo.service;
import com.example.demo.model.ProductModel;
import org.springframework.stereotype.Service;
import java.util.Arrays;
import java.util.List;

@Service
public class ProductService {
    // create a dummy list of products
    List<ProductModel> products = Arrays.asList(
            new ProductModel(101, "Iphone", 5000),
            new ProductModel(102, "Samsung", 4000),
            new ProductModel(103, "Techno", 3000),
            new ProductModel(104, "Infinity", 2000),
            new ProductModel(105, "Itel", 1000),
            new ProductModel(107, "Nokia", 50)
    );

    // return the list of products
    public List<ProductModel> getProduct() {
        return products;
    }
}  */

